# aws-iam-access-control
Enforcing least-privilege access control in AWS using IAM policies, user groups, and tag-based (attribute-based) conditions to restrict an intern's access to development EC2 resources while protecting production.
